description: Digee manual GPIO quadrature encoder (A/B phase pins)

compatible: "digee,md-encoder"

gpio-cells:
  - pin
  - flags

properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO phase A and phase B pins, in order [A, B]